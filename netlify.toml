[build]
  command = "npm run build"
  publish = ".next"

# Netlify auto-detects this, but pin it so SSR/ISR/route handlers work if detection ever misses.
[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20"